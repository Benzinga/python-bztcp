#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='bztcp',
    version='0.0.1',
    packages=find_packages(),
    scripts=[],
    install_requires=[],
    package_data={},
    include_package_data=True,
    author='Benzinga',
    description='Benzinga TCP library.',
    license='Proprietary',
    url='http://benzinga.com/',
)
